# Olivine transport-cost work — phase plan

Tracks the Olivine Group transport-cost integration (audit doc: "Olivine Group — Fleet Platform Fit-Gap & Architecture Assessment") phase by phase. Each phase is a small, independently reviewable slice — later phases never retrofit an earlier one's shape (see `TransportCostPostingService`'s own header for why it is parallel to, not a retrofit of, `AllocationService`).

| Phase | Scope | Status |
|---|---|---|
| **O1** | Import 3rd Party and Vansales rows as source evidence only (`tbltransportcostsourcerecords`), full provenance, no posting, no normalization. | Shipped. |
| **O2** | Normalization review: fuzzy-match transporter/vehicle identity, suggest-only, human-confirmed via `tblnormalizationreviewitems`. Never auto-merges. | Shipped. |
| **O3** | Post confirmed 3rd Party rows to the Allocation Ledger (`third-party-transport` cost category), reusing append-only/idempotency/reversal machinery. | Shipped. |
| **O4** | Business Stream → Vehicle/Transporter → time range report, sourced only from Allocation Ledger postings. Minimal slice: one screen, January 2026 default. | Shipped. |
| **Vansales posting** | Post Vansales retainer rows (`transport-retainer` cost category) to the ledger. Periodization Option A (declared-month `periodMonth`, `TOTAL` as amount) — see `VANSALES_PERIODIZATION_DECISION.md`. | **Shipped.** January 2026 reconciliation run: $0.00 unexplained residual (100% of the workbook's Vansales TOTAL sum accounted for), but the real January sheet has `TOTAL` populated on only 1 of 46 rows, so $0 actually posts for that month -- see the decision doc's "real-data finding" section, a data-quality/decision flag for Olivine, not an implementation gap. Not a phase number of its own; it is the remaining scope inside O3/O4 for the second of Section R2's three cost streams, deliberately not folded into "O3 shipped" above since 3rd Party and Vansales were genuinely separate decisions with separate risk. |
| **Swift** | Import + post contracted-delivery rows via one tolerant parser (required-column subset, reject-with-reason for genuinely missing columns — no per-month parser variants), `third-party-transport` cost category (see `OLIVINE_CURRENT_STATE_ASSESSMENT.md`, Section 6, for why it shares 3rd Party's category rather than a new one). | **Shipped.** January 2026 reconciliation run: $0.00 unexplained residual. But every Swift row structurally lacks a vehicle/transporter column in the source data, so every imported row skips at posting (`unresolved-vehicle-identity`) and **$0 actually posts** — see `SWIFT_POSTING_DECISION.md` for the full reasoning (why this can't be fixed by fabricating a vehicle or changing `AllocationPosting`'s schema) and `PHASE_O2_O3_O4_SUMMARY.md`'s "Swift import + posting" section for the real numbers. A data/attribution flag for Olivine, not an implementation gap. |
| **O5** | Depot STO (March–August, not just "May–August" as first assumed — see below) import + posting, `stock-transfer` cost category. One tolerant parser (`validateAndBuildDepotSto`) over the UNION of every column seen across six real sheets / four genuinely different shapes — a new import family, not an extension of the 3rd Party/Vansales importer, since Depot STO's schema is structurally different (a stock movement, not a delivery or retainer). | **Shipped.** Combined six-sheet reconciliation run: $0.00 unexplained residual. March/April/August rows carry a real, populated registration in the actual data and post normally; May has no registration column at all, and June/July's `REG` column exists but is essentially never populated in the real data (0%/1.2%) — both skip at posting (`unresolved-vehicle-identity`), a data fact rather than a schema one for June/July, corrected in `DEPOT_STO_DECISION.md` after being caught mid-slice. See `DEPOT_STO_DECISION.md` for the full record (including the sign-off-field/TOONNES provenance-only decisions and a mid-slice fix preserving May's five per-product quantity columns) and `PHASE_O2_O3_O4_SUMMARY.md`'s "Depot STO / O5" section for the real numbers. |
| **PODs** | Explicitly out of scope for the entire O-series. The audit's Section D found ~0 shared join keys between Sales invoice no. / Shipper reference / POD No. across these families — no hard join is safe, and none is attempted anywhere in O1–O5. | Not planned. Revisit only if Olivine supplies a real, verified join key. |
| **Fuel / cost-per-km for Olivine data** | Not planned. See `OLIVINE_CURRENT_STATE_ASSESSMENT.md`, Sections 9–10 — the workbook contains no fuel litres or reliable distance data for any of these source families, and Olivine's registrations are contracted vehicles (`tblcontractedvehicles`), not telematics-equipped owned vehicles. The platform's real `AllocationService.getCostPerKm` engine exists and already returns `null` rather than a fabricated figure when distance is unavailable — it should not be pointed at Olivine data without real per-truck distance/fuel input. | Not planned. Revisit only if Olivine supplies real per-truck fuel/distance data, as its own new import family and decision document. |

## Why Vansales posting and Depot STO are tracked separately

Section R2 of the audit named three transport cost streams to reconcile against the ledger: 3rd Party (delivered, O3), Vansales retainer (blocked on the periodization decision above), and Depot STO (deferred to O5). They were never one undifferentiated "remaining work" bucket — each has its own blocker (a business decision for Vansales; an entire missing import pipeline for Depot STO) and its own risk profile, so tracking them as one line would have hidden that difference. The `AllocationCostCategory` union already reserves distinct categories for both (`transport-retainer`, `stock-transfer`) so that whichever ships first does not have to retrofit the other's shape or invent a category under time pressure later — see `modules/finance/types/allocation.types.ts`'s doc comments on those two members.

## Updating this document

When Vansales posting or O5 (Depot STO) ships, move its row's Status to "Shipped" and add a one-line pointer to the delivery summary that shipped it (following `PHASE_O2_O3_O4_SUMMARY.md`'s own pattern), rather than opening a new phase-plan document.
